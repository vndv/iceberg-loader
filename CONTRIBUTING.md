# Contributing to iceberg-loader

Спасибо, что хотите помочь проекту! Ниже краткий гайд, как быстро начать.

## Быстрый старт
1) Клонируйте репозиторий  
2) Установите [Hatch](https://hatch.pypa.io/latest/) (используем его для окружений и команд):  
   ```bash
   pip install hatch
   ```
3) Установите зависимости и активируйте shell:  
   ```bash
   hatch shell
   ```
4) Запустите линтеры и тесты:  
   ```bash
   hatch run lint
   hatch run test
   ```

## Стиль кода
- Python 3.13, стиль проверяем `ruff`, форматирование `ruff format`.
- Добавляйте типы и короткие docstring к публичным функциям/классам.
- Логи: используйте `logging.getLogger(__name__)` и формат из `examples/*`.

## Тесты
- Новая фича → новый тест в `tests/`.
- Для примеров можно добавлять интеграционные проверки/фикстуры.

## Pull Request
- Опишите суть и мотивацию изменений.
- Приложите результаты тестов (`hatch run test`) и, если нужно, скрин/лог.
- Держите PR компактными; большие задачи разбивайте на части.

## Выпуски
- Публикация на PyPI автоматизирована через GitHub Actions (`release.yml`) по тегу `v*`.
- Перед релизом убедитесь, что CI зелёный.

Спасибо за участие! 🎉

